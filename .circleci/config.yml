version: 2.1

orbs:
  heroku: circleci/heroku@1.2.4

defaults: &defaults 
  working_directory: /home/circleci/project
  docker:
    - image: circleci/node

#A job is a series of steps that are used 
jobs: 
  build:
    <<: *defaults 
    steps:  #steps are a set of commands that are ran when running a job
      - checkout 
      - run: 
          name: Build OneApp App 
          command: npm install 
      - run:
          name: Test OneApp App  
          command: npm test 
      
      - persist_to_workspace:
          root: /home/circleci/project
          paths: .
              
#Workflow are the rules used to define a set of jobs.           
workflows:
  oneapp-pkg:
    jobs:
      - build 
      - heroku/deploy-via-git: 
          pre-steps:
<<<<<<< HEAD
            - run: export HEROKU_API_KEY=$HEROKU_API_KEY
            - run: export HEROKU_APP_NAME=$HEROKU_APP_NAME
            - run: echo $HEROKU_API_KEY
          requires:
            - build
=======
            - run: export HEROKU_API_KEY=${HEROKU_API_KEY}
            - run: export HEROKU_APP_NAME=${HEROKU_APP_NAME}
            - run: echo $HEROKU_API_KEY

>>>>>>> 0ceaa8bb88b9c22df2f7e97bc0621500064e1edc
