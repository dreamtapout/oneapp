version: 2

defaults: &defaults 
  # working_directory: ~/oneapp
  docker:
    - image: circleci/node

jobs: 
  test:
    <<: *defaults 
    steps:  
      - checkout 

      - run: 
          name: Application Build 
          command: npm install 
      
      - run:
          name: Application test  
          command: npm test 

workflows:
  version: 2
  test-deploy:
    jobs:
      - test  